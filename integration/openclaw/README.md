# OpenClaw Integration

Thin bridge to route OpenClaw-style natural language tasks to this repo's `frontdesk` workflows. No OpenClaw skill bodies are copied here.

See `contracts/bridge_contract.md` and `config/schema.json`.

